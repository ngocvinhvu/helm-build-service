apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "buildservice.fullname" . }}-test-connection"
  labels:
    {{- include "buildservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "buildservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
